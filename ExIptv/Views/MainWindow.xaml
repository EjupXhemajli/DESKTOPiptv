<Window x:Class="ExIptv.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vlc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
        xmlns:helpers="clr-namespace:ExIptv.Helpers"
        Title="EX-IPTV Desktop v0.9" Height="760" Width="1240"
        MinHeight="560" MinWidth="960"
        WindowStartupLocation="CenterScreen"
        Icon="/Assets/app_icon.png"
        KeyDown="OnWindowKeyDown"
        Background="{StaticResource BgBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- Toolbar -->
            <RowDefinition Height="*"/>      <!-- Hauptbereich -->
            <RowDefinition Height="Auto"/>   <!-- Statusleiste -->
        </Grid.RowDefinitions>

        <!-- ===== Toolbar ===== -->
        <Border x:Name="ToolbarBar" Grid.Row="0" Background="{StaticResource PanelBrush}" Padding="12,10"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="260"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Image Source="/Assets/logo_small.png" Height="30" VerticalAlignment="Center"
                       Margin="4,0,20,0" RenderOptions.BitmapScalingMode="HighQuality"/>

                <ComboBox Grid.Column="1" ItemsSource="{Binding Sources}"
                          SelectedItem="{Binding SelectedSource}" VerticalAlignment="Center"
                          DisplayMemberPath="Name"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="10,0,0,0">
                    <Button Content="+ Quelle" Style="{StaticResource AccentButton}"
                            Command="{Binding AddSourceCommand}" Margin="0,0,6,0"/>
                    <Button Content="Aktualisieren" Style="{StaticResource FlatButton}"
                            Command="{Binding RefreshSourceCommand}"
                            IsEnabled="{Binding HasSource}" Margin="0,0,6,0"/>
                    <Button Content="Löschen" Style="{StaticResource FlatButton}"
                            Command="{Binding DeleteSourceCommand}"
                            IsEnabled="{Binding HasSource}"/>
                </StackPanel>

                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="120" Height="4"
                                 Background="Transparent" Foreground="{StaticResource AccentBrush}"
                                 BorderThickness="0" Margin="0,0,12,0"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="⚙  Einstellungen" Style="{StaticResource FlatButton}"
                            Command="{Binding OpenSettingsCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ===== Hauptbereich: 3 Spalten ===== -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="CatCol" Width="240"/>       <!-- Kategorien -->
                <ColumnDefinition x:Name="ContentCol" Width="340"/>   <!-- Inhalte / Poster -->
                <ColumnDefinition x:Name="PlayerCol" Width="*"/>      <!-- Player -->
            </Grid.ColumnDefinitions>

            <!-- Kein-Quelle-Hinweis (über alles gelegt) -->
            <Border Grid.ColumnSpan="3" Panel.ZIndex="10" Background="{StaticResource BgBrush}"
                    Visibility="{Binding HasSource, Converter={StaticResource BoolToVis}, ConverterParameter=invert}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image Source="/Assets/logo.png" Width="360" HorizontalAlignment="Center"
                           Margin="0,0,0,28" RenderOptions.BitmapScalingMode="HighQuality"/>
                    <TextBlock Text="Keine Quelle vorhanden" FontSize="20" FontWeight="SemiBold"
                               HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="Füge einen Xtream-Zugang oder eine M3U-Playlist hinzu, um zu starten."
                               Foreground="{StaticResource TextDimBrush}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <Button Content="+ Quelle hinzufügen" Style="{StaticResource AccentButton}"
                            Command="{Binding AddSourceCommand}" HorizontalAlignment="Center" Padding="18,10"/>
                </StackPanel>
            </Border>

            <!-- Spalte 1: Sektionen + Kategorien -->
            <Border Grid.Column="0" Background="{StaticResource PanelBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8,10">
                        <RadioButton Content="Live" Style="{StaticResource SectionButton}"
                                     IsChecked="True" Command="{Binding ShowLiveCommand}"/>
                        <RadioButton Content="Filme" Style="{StaticResource SectionButton}"
                                     Command="{Binding ShowMoviesCommand}"/>
                        <RadioButton Content="Serien" Style="{StaticResource SectionButton}"
                                     Command="{Binding ShowSeriesCommand}"/>
                    </StackPanel>

                    <TextBlock DockPanel.Dock="Top" Text="KATEGORIEN" FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource TextDimBrush}" Margin="14,6,0,6"/>

                    <ListBox Style="{StaticResource CleanListBox}"
                             ItemContainerStyle="{StaticResource RowItem}"
                             ItemsSource="{Binding Categories}"
                             SelectedItem="{Binding SelectedCategory}"
                             Margin="6,0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Grid.Column="1" Text="{Binding ItemCount}"
                                               Foreground="{StaticResource TextDimBrush}" FontSize="11"
                                               VerticalAlignment="Center" Margin="6,0,0,0"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <!-- Spalte 2: Suche + Inhaltsliste (mit Episoden-Overlay, rein WPF – kein Airspace-Konflikt) -->
            <Border Grid.Column="1" Background="{StaticResource BgBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                <Grid>
                    <!-- Inhalte: Live -> Liste, Filme/Serien -> Poster-Grid -->
                    <DockPanel Margin="10">
                        <StackPanel DockPanel.Dock="Top">
                            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,0,8" Tag="Suchen…"/>
                            <ComboBox x:Name="SortBox" Margin="0,0,0,10"
                                      ItemsSource="{Binding SortModes}" DisplayMemberPath="Label"
                                      SelectedValuePath="Value" SelectedValue="{Binding SelectedSortMode}"/>
                        </StackPanel>

                        <Grid>
                            <!-- Live: klassische Liste -->
                            <ListBox x:Name="LiveList"
                                     Style="{StaticResource CleanListBox}"
                                     ItemContainerStyle="{StaticResource RowItem}"
                                     ItemsSource="{Binding Items}"
                                     SelectedItem="{Binding SelectedItem}"
                                     SelectionChanged="OnLiveSelectionChanged"
                                     VirtualizingStackPanel.IsVirtualizing="True"
                                     VirtualizingStackPanel.VirtualizationMode="Recycling">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis" FontSize="13"/>
                                                <TextBlock Text="{Binding Subtitle}" Foreground="{StaticResource TextDimBrush}"
                                                           FontSize="11"
                                                           Visibility="{Binding Subtitle, Converter={StaticResource BoolToVis}}"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="›" FontSize="16"
                                                       Foreground="{StaticResource TextDimBrush}"
                                                       VerticalAlignment="Center"
                                                       Visibility="{Binding IsSeriesContainer, Converter={StaticResource BoolToVis}}"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <!-- Filme/Serien: Poster-Grid -->
                            <ListBox x:Name="PosterGrid"
                                     Style="{StaticResource CleanListBox}"
                                     ItemsSource="{Binding Items}"
                                     SelectedItem="{Binding SelectedItem}"
                                     MouseDoubleClick="OnContentDoubleClick"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                     ScrollViewer.CanContentScroll="False">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Margin" Value="6"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border x:Name="pb" CornerRadius="8" BorderThickness="2"
                                                            BorderBrush="Transparent">
                                                        <ContentPresenter/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="pb" Property="BorderBrush"
                                                                    Value="{StaticResource AccentBrush}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="pb" Property="BorderBrush"
                                                                    Value="{StaticResource AccentBrush}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Width="150">
                                            <Border Width="150" Height="220" CornerRadius="6"
                                                    Background="{StaticResource PanelAltBrush}" ClipToBounds="True">
                                                <Grid>
                                                    <TextBlock Text="🎬" FontSize="42" HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"
                                                               Foreground="{StaticResource TextDimBrush}"/>
                                                    <Image helpers:ImageLoader.SourceUrl="{Binding LogoUrl}"
                                                           Stretch="UniformToFill"
                                                           RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    <TextBlock Text="›" FontSize="22" FontWeight="Bold"
                                                               HorizontalAlignment="Right" VerticalAlignment="Top"
                                                               Margin="0,4,8,0" Foreground="White"
                                                               Visibility="{Binding IsSeriesContainer, Converter={StaticResource BoolToVis}}"/>
                                                </Grid>
                                            </Border>
                                            <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"
                                                       TextWrapping="Wrap" MaxHeight="34" FontSize="12"
                                                       TextAlignment="Center" Margin="2,6,2,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </DockPanel>

                    <!-- Episoden-Ansicht (überlagert die Inhaltsliste bei Serienauswahl) -->
                    <Border Background="{StaticResource BgBrush}"
                            Visibility="{Binding ShowEpisodes, Converter={StaticResource BoolToVis}}">
                        <DockPanel Margin="10">
                            <Button DockPanel.Dock="Top" Content="‹ Zurück" Style="{StaticResource FlatButton}"
                                    HorizontalAlignment="Left" Command="{Binding CloseEpisodesCommand}"
                                    Margin="0,0,0,8"/>
                            <TextBlock DockPanel.Dock="Top" Text="{Binding EpisodesTitle}" FontSize="15"
                                       FontWeight="Bold" TextTrimming="CharacterEllipsis" Margin="2,0,0,10"/>
                            <ListBox Style="{StaticResource CleanListBox}"
                                     ItemContainerStyle="{StaticResource RowItem}"
                                     ItemsSource="{Binding Episodes}"
                                     MouseDoubleClick="OnEpisodeDoubleClick"
                                     VirtualizingStackPanel.IsVirtualizing="True"
                                     VirtualizingStackPanel.VirtualizationMode="Recycling">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Display}" FontSize="13" TextTrimming="CharacterEllipsis"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </DockPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Spalte 3: Player -->
            <Grid Grid.Column="2" Background="Black">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- VideoView -->
                <vlc:VideoView x:Name="VideoView" Grid.Row="0"/>

                <!-- Platzhalter, wenn nichts läuft -->
                <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="{Binding IsPlaying, Converter={StaticResource BoolToVis}, ConverterParameter=invert}"
                            IsHitTestVisible="False">
                    <TextBlock Text="▶" FontSize="52" Foreground="#FF3A4150" HorizontalAlignment="Center"/>
                    <TextBlock Text="Sender oder Titel doppelklicken zum Abspielen"
                               Foreground="#FF556070" Margin="0,10,0,0"/>
                </StackPanel>

                <!-- Player-Leiste (Steuerung unter dem Video – Airspace-sicher) -->
                <Border x:Name="PlayerBar" Grid.Row="1" Background="{StaticResource PanelBrush}" Padding="14,8">
                    <StackPanel>
                        <!-- Spulleiste: nur bei Filmen/Serien -->
                        <Grid Margin="0,0,0,6"
                              Visibility="{Binding CanSeek, Converter={StaticResource BoolToVis}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding PlaybackTimeText}" VerticalAlignment="Center"
                                       FontSize="11" Foreground="{StaticResource TextDimBrush}" Margin="0,0,10,0"/>
                            <Slider x:Name="SeekSlider" Grid.Column="1" Minimum="0" Maximum="1"
                                    VerticalAlignment="Center"
                                    IsMoveToPointEnabled="True"
                                    Thumb.DragStarted="OnSeekDragStarted"
                                    Thumb.DragCompleted="OnSeekDragCompleted"
                                    Value="{Binding PlaybackPosition}"/>
                            <TextBlock Grid.Column="2" Text="{Binding PlaybackLengthText}" VerticalAlignment="Center"
                                       FontSize="11" Foreground="{StaticResource TextDimBrush}" Margin="10,0,0,0"/>
                        </Grid>

                        <!-- Steuerzeile -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Orientation="Horizontal">
                                <Button Content="↺ 15" Style="{StaticResource FlatButton}" FontSize="13"
                                        Command="{Binding SeekBackwardCommand}" Padding="10,4" Margin="0,0,6,0"
                                        Visibility="{Binding CanSeek, Converter={StaticResource BoolToVis}}"/>
                                <Button Content="⏯" Style="{StaticResource FlatButton}" FontSize="16"
                                        Command="{Binding TogglePauseCommand}" Margin="0,0,6,0" Padding="14,4"/>
                                <Button Content="⏹" Style="{StaticResource FlatButton}" FontSize="16"
                                        Command="{Binding StopPlaybackCommand}" Padding="14,4"/>
                                <Button Content="15 ↻" Style="{StaticResource FlatButton}" FontSize="13"
                                        Command="{Binding SeekForwardCommand}" Padding="10,4" Margin="6,0,0,0"
                                        Visibility="{Binding CanSeek, Converter={StaticResource BoolToVis}}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0">
                                <TextBlock Text="{Binding NowPlaying}" TextTrimming="CharacterEllipsis"
                                           FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="{Binding PlayerStatus}" Foreground="{StaticResource TextDimBrush}"
                                           FontSize="11"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <!-- Bildformat (auch für Live) -->
                                <ComboBox Width="86" Margin="0,0,6,0" ToolTip="Bildformat"
                                          ItemsSource="{Binding AspectRatios}"
                                          SelectedItem="{Binding SelectedAspectRatio}"/>
                                <!-- Tonspur -->
                                <ComboBox Width="104" Margin="0,0,6,0" ToolTip="Tonspur"
                                          ItemsSource="{Binding AudioTracks}" DisplayMemberPath="Name"
                                          SelectedItem="{Binding SelectedAudioTrack}"
                                          DropDownOpened="OnTracksDropDownOpened"/>
                                <!-- Untertitel -->
                                <ComboBox Width="104" Margin="0,0,12,0" ToolTip="Untertitel"
                                          ItemsSource="{Binding SubtitleTracks}" DisplayMemberPath="Name"
                                          SelectedItem="{Binding SelectedSubtitleTrack}"
                                          DropDownOpened="OnTracksDropDownOpened"/>
                                <TextBlock Text="🔊" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <Slider Width="90" Minimum="0" Maximum="100"
                                        Value="{Binding Volume}" VerticalAlignment="Center"/>
                                <Button Content="⛶" Style="{StaticResource FlatButton}" FontSize="15"
                                        Click="OnToggleFullscreen" Padding="10,4" Margin="12,0,0,0"
                                        ToolTip="Vollbild (F11) – Esc beendet"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Verstellbare Spaltenbreiten (liegen über den Spaltenrändern) -->
            <GridSplitter x:Name="SplitCat" Grid.Column="0" Width="6" HorizontalAlignment="Right"
                          VerticalAlignment="Stretch" Background="Transparent"
                          ResizeBehavior="CurrentAndNext" ResizeDirection="Columns"
                          DragCompleted="OnSplitterDragCompleted"/>
            <GridSplitter x:Name="SplitContent" Grid.Column="1" Width="6" HorizontalAlignment="Right"
                          VerticalAlignment="Stretch" Background="Transparent"
                          ResizeBehavior="CurrentAndNext" ResizeDirection="Columns"
                          DragCompleted="OnSplitterDragCompleted"/>
        </Grid>

        <!-- ===== Statusleiste ===== -->
        <Border x:Name="StatusBar" Grid.Row="2" Background="{StaticResource PanelBrush}" Padding="14,6"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource TextDimBrush}" FontSize="12"/>
                <TextBlock Grid.Column="1" Text="EX-IPTV v0.9 · Dialog-Fix + Diagnose"
                           Foreground="{StaticResource AccentBrush}" FontSize="12" FontWeight="SemiBold"/>
            </Grid>
        </Border>
    </Grid>
</Window>
